# Aletheon project-level Codex config.
# Secrets, API keys, personal paths belong in config.local.toml (gitignored).

[project]
name = "aletheon"
integration_branch = "dev"

[workflow]
delete_merged_branches = true

[mcp_servers.aletheon-monitor]
command = "aletheon-monitor"
