schema_version = 1
id = "api_error_mapping"
category = "multifile_change"
fixture = "api_error_mapping"
prompt = "实现跨文件 typed error 映射：domain 的 Missing→404 且稳定 message 为 `missing`、Conflict→409 且稳定 message 为 `conflict`、Invalid→422 且稳定 message 为 `invalid`；wire response 保留 code 与该稳定 message。不要修改测试或清单。"
timeout_secs = 300
acceptance_commands = [["cargo", "test", "--quiet"]]
forbidden_paths = ["Cargo.toml", "Cargo.lock", "tests/"]
required_changed_paths = ["src/"]
expected_terminal = "verified"
resource_checks = ["no_descendant_processes"]

[setup]
