schema_version = 1
id = "state_transition"
category = "regression_test"
fixture = "state_transition"
prompt = "Reject illegal state transitions while keeping valid transitions unchanged."
timeout_secs = 300
acceptance_commands = [["cargo", "test", "--quiet"]]
forbidden_paths = ["Cargo.toml", "Cargo.lock"]
required_changed_paths = ["src/"]
expected_terminal = "verified"
resource_checks = ["no_descendant_processes"]

[setup]
