# tools

Tool registration and execution framework for the Aletheon agent.

## Overview

Provides the tool abstraction layer that allows agents to use external tools.

## Key Types

- `Tool` — Tool trait for defining tools
- `ToolResult` — Tool execution result
- `ToolRegistry` — Registry of available tools
